import json
import boto3

dynamodb = boto3.resource('dynamodb')
table = dynamodb.Table('Info_Table')

def lambda_handler(event, context):
    # TODO implement
    response=table.get_item(Key={'ID':'1870'})
    return {
        'statusCode': 200,
        'body': response['Item']
    }
